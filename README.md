# BetterBan

## Purpose

The Northstar Docker image recreates the R2Northstar folder on each startup, which contains banlist.txt. This mod is essentially spyglass-lite. This requires your own backend that the mod can query.